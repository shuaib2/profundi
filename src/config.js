// Configuration file for the application
// This allows us to access configuration values in one place

// We've switched from Google Maps API to OpenStreetMap's Nominatim API
// No API key or billing is required for OpenStreetMap

// Nominatim API configuration
export const NOMINATIM_API_URL = 'https://nominatim.openstreetmap.org/search';
export const NOMINATIM_USER_AGENT = 'Profundi Location Search';

// Other configuration values can be added here
